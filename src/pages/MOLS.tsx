import React from "react";
import EntityTemplate from "./EntityTemplate";

const MOLS = () => {
    return <EntityTemplate entityName="MOLS" logoSrc="/src/images/MOLS.png" providers={["MOR", "MOTRI"]} />;
};

export default MOLS;
