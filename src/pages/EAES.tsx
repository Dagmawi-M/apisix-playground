import React from "react";
import EntityTemplate from "./EntityTemplate";

const EAES = () => {
    return <EntityTemplate entityName="EAES" logoSrc="/src/images/EAES.png" providers={["NID", "DARS", "MOR", "MOTRI", "MOLS"]} />;
};

export default EAES;
